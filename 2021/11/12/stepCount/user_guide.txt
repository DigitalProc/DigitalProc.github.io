app内没有可调的输入参数

这个app只能在安卓系统上使用，没有ios版本🤣

打开app之后直接点击“写文件”，就会自动把时间和加速度传感器信息写入一个csv文件

点击“停止写文件”就会停止写入。

csv文件的位置在 /Document/acc_data/ 文件夹内，每次写入的时候会生成一个新的文件，文件名是{开始写文件的时间}.csv。

PS : 因为需要写入文件，所以会请求读写文件的权限，同学们放心同意就可以